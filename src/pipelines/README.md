# src/pipelines — Flujos de datos

Pipelines de extracción → transformación → carga/análisis. Orquestan `db/` (fuentes),
`utils/` (limpieza) y las consultas de `data/sql/`. Lo estable vive aquí; el tanteo, en `sandbox/`.
