# src/config — Configuración

Lee credenciales y parámetros desde el `.env` (`python-dotenv`). Un **prefijo por motor**
(`MSSQL_`, `PG_`, `ORACLE_`), nunca un genérico `DB_`. Cero credenciales hardcodeadas.
